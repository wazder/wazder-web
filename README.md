# wazder.com

Minimal "Under Maintenance" page.

## Deploy

```bash
npx wrangler pages deploy . --project-name=wazder-web --commit-dirty=true
```

## Yapı

```
.
└── index.html
```

Single file. Favicon embedded as inline SVG.
