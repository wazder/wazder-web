---
const primaryEmail = 'tatarhasan09@gmail.com';

const profiles = [
  { label: 'GitHub', href: 'https://github.com/wazder', handle: '@wazder' },
  { label: 'LinkedIn', href: 'https://linkedin.com/in/wazder', handle: 'in/wazder' },
];
---

<section id="contact" class="py-32 px-6">
  <div class="mx-auto max-w-3xl">
    <h2 class="text-sm uppercase tracking-[0.3em] text-white/40 mb-10">Contact</h2>

    <p class="text-base text-white/60 max-w-xl leading-relaxed mb-10">
      Open to AI/ML, full-stack, and product engagements. Easiest to reach by email.
    </p>

    <a
      href={`mailto:${primaryEmail}`}
      class="inline-block text-xl sm:text-2xl md:text-3xl font-light hover:text-white text-white/80 transition-colors break-all"
    >
      {primaryEmail}
    </a>

    <div class="mt-12 grid sm:grid-cols-2 gap-x-12 gap-y-3">
      <ul class="space-y-3">
        {profiles.map((p) => (
          <li class="grid grid-cols-[80px_1fr] gap-4 items-baseline">
            <span class="text-xs uppercase tracking-[0.2em] text-white/40">{p.label}</span>
            <a
              href={p.href}
              target="_blank"
              rel="noreferrer"
              class="text-sm text-white/70 hover:text-white transition-colors"
            >
              {p.handle} ↗
            </a>
          </li>
        ))}
      </ul>

      <ul class="space-y-3">
        <li class="grid grid-cols-[80px_1fr] gap-4 items-baseline">
          <span class="text-xs uppercase tracking-[0.2em] text-white/40">CLL</span>
          <a
            href="https://camellayer.com"
            target="_blank"
            rel="noreferrer"
            class="text-sm text-white/70 hover:text-white transition-colors"
          >
            camellayer.com ↗
          </a>
        </li>
        <li class="grid grid-cols-[80px_1fr] gap-4 items-baseline">
          <span class="text-xs uppercase tracking-[0.2em] text-white/40">CV</span>
          <a
            href="/hasan-tatar-cv.pdf"
            download
            class="text-sm text-white/70 hover:text-white transition-colors"
          >
            Download ↓
          </a>
        </li>
      </ul>
    </div>
  </div>
</section>
